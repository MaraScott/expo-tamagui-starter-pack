import { Paragraph, H2, styled } from 'tamagui'

export const Muted = styled(Paragraph, {
  color: '$muted',
  size: 3,
})

export const GlowH2 = styled(H2, {
  color: '$glow',
  size: 6,
  mb: 12,
})
